Changes In app.component.html,

<div class="form-group">
      <label>Username</label>
      <input type="text" [class.is-invalid]="registrationForm.get('userName').invalid && registrationForm.get('userName').touched" formControlName="userName" class="form-control">
      <div *ngIf="registrationForm.get('userName').invalid && registrationForm.get('userName').touched">
        <small class="text-danger" *ngIf="registrationForm.get('userName').errors?.required">Username is required</small>                      //If it doesn't work then try,registrationForm.get('userName').errors.required or registrationForm.get('userName').errors['required'] or registrationForm.get('userName').hasError('required')
        <small class="text-danger" *ngIf="registrationForm.get('userName').errors?.minlength">Username must be at least 3 characters</small>    //Try the above approaches if this also doesn't work.
      </div>
    </div>
----------------------------
Changes in app.component.ts,
registrationForm = this.fb.group({
    userName: ['', [Validators.required, Validators.minLength(3)]],                 //Here the change is present.
    password: [''],
    confirmPassword: [''],
    address: this.fb.group({
      ...
     }) 
     });


------------------------------------------------------------------------------------
As writing registrationForm.get() is a repetitive process, so we can put getter methods in app.component.html
get userName() {
    return this.registrationForm.get('userName');
  }

So now in app.component.html,
<form [formGroup]="registrationForm">
    <div class="form-group">
      <label>Username</label>
       <input type="text" [class.is-invalid]="userName.invalid && userName.touched" formControlName="userName" class="form-control">
      <div *ngIf="userName.invalid && userName.touched">
        <small class="text-danger" *ngIf="userName.errors?.required">Username is required</small>
        <small class="text-danger" *ngIf="userName.errors?.minlength">Username must be at least 3 characters</small>
      </div>
   </div>
</form>